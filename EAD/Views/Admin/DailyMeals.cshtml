



@model List<DailyMenuViewModel>

<!-- Link the Styles -->
<link rel="stylesheet" href="~/css/AdminHome.css">
<!-- FontAwesome -->

<div class="page-container fade-in">
    <!-- Header: Title + Add Button -->
    <header class="page-header">
        <div class="page-title">
            <h2>Daily Meal Management</h2>
            <p>Manage daily meals for students and staff</p>
        </div>
        <a href="/Admin/SetDailyMeals" class="btn-add">
            <i class="fas fa-utensils"></i> Set daily meals
        </a>
    </header>

    <!-- Content Card -->
    <div class="data-card">
        @if (Model.Count == 0)
        {
            <div class="empty-state">
                <i class="fas fa-hamburger"></i>
                <h3>No meals found</h3>
                <p>Click the "Add Meal" button to get started.</p>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="styled-table">
                    <thead>
                        <tr>
                            <th>#ID</th>
                            <th>Day </th>
                            <th>Type</th>
                            <th>Name</th>

                            <th class="text-right">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td>#@user.Id</td>
                                <td >@user.DayOfWeek</td>
                                <td>@user.MealType</td>
                                <td >@user.MealItemName</td>
                                <td>
                                    <div class="action-buttons">
                                        <a href="/Admin/EditDailyMeal/@user.Id" class="btn-action btn-edit">
                                            <i class="fas fa-pen"></i> Edit
                                        </a>
                                        <a href="/Admin/DeleteDailyMeal/@user.Id" class="btn-action btn-delete" onclick="return confirm('Are you sure you want to delete this user?');">
                                            <i class="fas fa-trash"></i> Delete
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>